#include<bits/stdc++.h>
using namespace std;

int main() {
    int i;
    // for intialization; condition; updation
    for (i = 1; i <= 200; i = i + 1/*we can also write i++ instead of i = i + 1*/) {
        cout << "Somesh " << i << endl;
    }
    cout << i << endl;

    return 0;
}

